
// Spring Boot 3.2 + Angular v17: Paso 2 -> Modelo (interfaz)
// la Diferencia con una clase es que la interfaz modela un JSON
export interface Clientes {
    nif: string
    nombre: string
    edad: number
    sexo: number
}